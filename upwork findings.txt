Hi Najab — We can provide most of what you’re asking for, and I want to clarify a couple of items, so we’re aligned on what’s realistic given this needs to work across many client sites.



The goal of these briefs is to generate a ready-to-implement SEO reoptimization plan for a specific URL by combining GSC performance data, client brand/voice context, and keyword targets. The automation removes manual data gathering and outputs a standardized Google Doc brief (current state + recommended metadata/headings) that a writer/strategist can execute with minimal edits. Success means each brief is accurate and actionable—improving search performance and LLM visibility while staying aligned to the client’s positioning and audience.



• 10–15 historical URLs/content samples: Yes — we’ll send a set for testing/validation. I’m currently collecting these from the team. Where possible, we’ll include examples that already have approved briefs, so you have clear “starting inputs” and an “expected output” reference for validation.
• High-quality vs underperforming: We can provide a practical split (pages that are currently performing vs not), but since performance baselines vary by client/site, we’ll treat this primarily as a testing set rather than a strict universal benchmark.
• Internal SEO/content guidelines: We don’t have a single universal internal SEO guide because this is a multi-client environment. Instead, we’re providing brief acceptance criteria as the build requirements, and tone/brand guidance will be applied dynamically from the client profile.
• Output tools + access: We’ll work on getting you into the right system/access path. If needed, we can coordinate internally for GSC/API permissions.
• Feedback during refinement: We can commit to a 1–3 business-day SLA for feedback during build/refinement cycles.



If you’re aligned with the above, we should be able to lock the build plan and move into implementation as soon as access and the test URL set are in place. I'll arrange a meeting for all of us this week.



Thanks,
Claudia